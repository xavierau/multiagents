# MultiAgents Framework
multiagents>=0.1.0

# Core dependencies (included with multiagents)
redis>=5.0.0
pydantic>=2.0.0
structlog>=23.0.0

# Development dependencies
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0

# Optional: Enhanced logging
colorlog>=6.0.0